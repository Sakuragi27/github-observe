{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npm run build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"]
}
